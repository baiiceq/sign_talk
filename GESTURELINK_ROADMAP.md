# GestureLink 端到端落地路线（基于当前项目）

## 1. 现状总览（当前仓库已具备）

当前项目已经具备一个本地实时识别闭环：

1. 摄像头采集视频帧（`main.py`）。
2. MediaPipe 提取关键点（`src/gesture_detector/mediapipe_detector.py`）。
3. 特征提取并形成时序特征（`src/gesture_detector/feature_extractor.py`）。
4. 载入训练好的 BiLSTM 模型进行分类（`src/gesture_detector/gesture_classifier.py`）。
5. 将识别结果映射到鼠标/键盘命令执行（`src/control/command_executor.py`）。
6. 在画面上实时显示 FPS、手势信息、历史记录（`main.py`）。

这说明：**“实时手势识别 + 本机控制”主链路已经存在**，可以作为 GestureLink 的基础版本。

---

## 2. 与目标效果相比，主要差距

你的目标描述是一个更完整的工程化系统：

- 自建数据集 + 训练流程标准化。
- 前后端通信，把识别结果推送给 Web/应用层。
- 电脑操作不仅在本地脚本里触发，还可由后端策略控制。
- 具备可视化、低延迟、可扩展、可部署能力。

当前项目的缺口主要在：

1. 缺少独立的**数据采集与标注管线**（目前以集成说明为主）。
2. 缺少可复现的**训练脚本与模型版本管理**。
3. 缺少**后端服务层（API/WebSocket）**。
4. 缺少**前端实时面板**（识别状态、置信度曲线、日志回放）。
5. 缺少系统化的**性能优化与部署方案**。

---

## 3. 建议的目标架构（GestureLink）

建议拆成 4 层：

1. **Vision 引擎层（Python）**
   - 摄像头输入 → MediaPipe → 特征提取 → 序列分类。
   - 输出统一事件：`{gesture, confidence, ts, landmarks}`。

2. **服务层（Backend）**
   - FastAPI + WebSocket。
   - 提供 `/ws/gesture`（实时推流）和 `/api/command`（控制策略）。

3. **执行层（Action Runtime）**
   - 负责翻页、游戏操作、快捷键。
   - 做节流、防抖、权限控制（白名单应用）。

4. **展示层（Frontend）**
   - 实时显示视频流/关键点/手势概率。
   - 可配置映射规则（某手势绑定某动作）。

---

## 4. 分阶段实施计划

### Phase A（1~2 周）：打稳识别基础

- 固化标签体系：先从 6~10 个高区分手势开始。
- 建立数据目录规范：`data/raw`, `data/processed`, `data/splits`。
- 新增采集脚本：每个手势多人、多光照、多背景采样。
- 训练脚本化：
  - `train.py`（训练）
  - `eval.py`（混淆矩阵、F1、延迟）
- 产出第一版基准模型：满足 >90% 离线准确率（按验证集）。

### Phase B（1 周）：实时性与稳定性

- 引入时序平滑：滑窗投票 + 置信度门限。
- 加入触发策略：
  - 冷却时间（cooldown）
  - 手势持续时长阈值（hold）
- 优化端到端时延：目标 80~150ms。
- 引入日志埋点：每帧耗时（检测/推理/渲染）。

### Phase C（1 周）：后端与前端联动

- Backend（FastAPI）：
  - WebSocket 推送实时手势。
  - REST 管理映射规则与配置。
- Frontend（React/Vue 任一）：
  - 状态面板：当前手势、置信度、FPS。
  - 映射面板：Gesture → Action 可视化配置。

### Phase D（1 周）：控制与部署

- 执行层支持多场景模板：
  - 演示翻页（PPT）
  - 游戏控制（按键映射）
  - 通用桌面快捷操作
- 打包与部署：
  - 本地桌面版（PyInstaller）或 Docker 服务版。
- 可靠性：崩溃恢复、异常重连、模型热切换。

---

## 5. 基于当前仓库的最小改造清单

建议优先新增这些文件：

1. `scripts/collect_dataset.py`
   - 自动采集关键点与标签，落盘为 `.npy/.csv`。

2. `scripts/train_model.py`
   - 读取数据、训练 BiLSTM、导出 `models/*.keras`。

3. `backend/app.py`
   - 提供 WebSocket 推送识别事件。

4. `frontend/`（可先最简）
   - 页面展示实时手势和日志。

5. `src/control/policy_engine.py`
   - 把“识别结果”与“最终动作触发”解耦（便于防抖与策略切换）。

---

## 6. 关键工程指标（建议验收标准）

- 实时性：平均端到端延迟 <150ms。
- 稳定性：连续运行 2 小时无崩溃。
- 准确率：核心手势 Top-1 >90%。
- 误触率：常见场景下误触 <3%。
- 可维护性：配置可热更新，日志可追踪。

---

## 7. 结论

当前项目已经是一个**可运行的基础骨架**，离你定义的 GestureLink 目标只差“工程化闭环”：

- 数据闭环（采集-训练-评估）
- 服务闭环（后端推流-前端可视化）
- 控制闭环（策略层-动作执行层）

按上面的分阶段方案推进，通常 3~5 周可以做出一个可演示、可扩展的端到端 AI 项目版本。
